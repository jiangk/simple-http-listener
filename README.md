simple-http-listener
====================
A basic http server in node.js that logs all incoming requests in raw format
Returns 200 OK to all requests

By default, writes to console and log.txt

Uses [parsley](https://github.com/substack/node-parsley) to obtain the raw HTTP requests
